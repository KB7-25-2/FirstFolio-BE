<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.firstfolio.curriculum.mapper.AdminAuditLogMapper">

    <insert id="insert">
        INSERT INTO admin_audit_logs (
            actor_user_id,
            action_type,
            entity_type,
            entity_id,
            before_json,
            after_json,
            request_id,
            result_status,
            created_at
        ) VALUES (
            #{actorUserId},
            #{actionType},
            #{entityType},
            #{entityId},
            #{beforeJson,jdbcType=VARCHAR},
            #{afterJson,jdbcType=VARCHAR},
            #{requestId},
            'SUCCESS',
            #{createdAt}
        )
    </insert>

</mapper>
